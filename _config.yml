#baseurl: /verkefni2 Can be used to setup for links if need be
collections:
  companies:
    output: true
defaults:
  - scope:
      path: ""
      type: "companies"
    values:
      layout: "publisher"
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "games"
  - scope:
      path: ""
    values:
      layout: "default"
plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag


      